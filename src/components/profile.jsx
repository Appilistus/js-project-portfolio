const profileImages = {
    main: "",
    contact1: "",
    contact2: ""
}

export const profileImg = (props) => {
    return (
        <profileImg src={props.src} alt=""/>
    )
}

export default profileImg;